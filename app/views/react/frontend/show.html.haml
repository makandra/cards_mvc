%body
  #app

  = javascript_include_tag 'react/app'
  :javascript
    setCsrfToken(#{form_authenticity_token.to_json});
    ReactDOM.render(React.createElement(App), document.getElementById('app'));
