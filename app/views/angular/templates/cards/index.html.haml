.row
  %h1
    .pull-right(ng-show='sessionCtrl.signedIn')
      %a.btn.btn-default(ng-href='#/cards/new')
        New card
    Cards

%p

.row
  %form
    .form-group
      %input.form-control(type='search' placeholder='Search…' ng-model='ctrl.searchQuery' ng-change='ctrl.search(ctrl.searchQuery)')


%p

.row
  .text-center(ng-show='ctrl.loading')
    %i.material-icons.spin
      cached

  %table.table(ng-hide='ctrl.loading')
    %tr(ng-repeat='card in ctrl.cards')
      %td
        %a(ng-href='#/cards/{{ card.id }}') {{ card.title }}
      %td.text-right(ng-show='sessionCtrl.signedIn')
        %a.btn.btn-default(ng-href='#/cards/{{ card.id }}/edit') Edit

  %pagination(info='ctrl.cards.$metadata.pagination' go-to='ctrl.goToPage(page)' ng-hide='ctrl.loading')
